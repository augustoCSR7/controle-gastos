# Configurações específicas para deployment no Railway
# Substitua pelos seus valores reais do MongoDB Atlas

# MongoDB Atlas Connection String (principal)
MONGODB_URL=mongodb+srv://USERNAME:PASSWORD@cluster0.xxxxx.mongodb.net/controle_gastos?retryWrites=true&w=majority&ssl=true&tlsAllowInvalidCertificates=true

# Nome do database
MONGODB_DATABASE=controle_gastos

# Port (Railway define automaticamente)
PORT=8000

# Environment
ENVIRONMENT=production

# SSL Settings específicos para Railway
SSL_CERT_REQS=CERT_NONE
SSL_MATCH_HOSTNAME=false
CONNECT_TIMEOUT_MS=30000
SERVER_SELECTION_TIMEOUT_MS=30000
SOCKET_TIMEOUT_MS=45000

# MongoDB Atlas IP Whitelist
# No MongoDB Atlas, adicione estes IPs à whitelist:
# 0.0.0.0/0 (permite todos os IPs - só para teste)
# Ou consulte os IPs específicos do Railway na documentação
